import * as d3 from 'd3';

export function createVisu1 (match_events: any) {
  console.log(match_events)



  // Scaling
  const scaleX = d3.scaleLinear()
  const scaleY = d3.scaleLinear()

  d3.select('body')
}
